[package]
name = "packet-match-fuzz"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "KMP-based packet pattern matcher with streaming support and fuzzing utilities"

[dependencies]
rand = "0.8"
